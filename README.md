# nice-config-prettier

Shared Prettier configuration for the `nice-*` ecosystem.

## Usage

In `package.json`:

```json
{
  "prettier": "nice-config-prettier"
}
```

`prettier` is an optional peer dependency provided by the consuming package.
